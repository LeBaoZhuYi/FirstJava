题目一：
写一个链表的转置，也就是从前往后变成从后往前，分别用递归和非递归实现（不用栈）

题目二：
实现一个栈，有pop，push以及一个min函数，min函数的返回值是这个栈中元素的最小值，并且三个函数的时间和空间复杂度均是O(1)

题目三：
实现一个函数，判断一个链表是否有环，判断环的起点位置

题目四：
有一颗树（不一定是二叉树），每个结点有权值，判断这颗树能否切成两颗树（切断某一根树枝），使得他们的权值和相等
